# automated_weather_pipeline
Build a automated pipelines with Airflow in which I have get the data from OpenWeatherApi for data of any city, then after extraction I have cleaned it and transform into a dictionary , and then store the valuable data to a Json file in a docker container.
